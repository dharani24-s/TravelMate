import { useState } from "react"; import { Link, useLocation, useNavigate } from "react-router-dom"; import { Plane } from "lucide-react"; import { useApp } from "../context/AppContext";
export default function Login(){const {login}=useApp();const nav=useNavigate();const loc=useLocation();const [form,setForm]=useState({email:"",password:""});const [error,setError]=useState("");const submit=e=>{e.preventDefault();if(!/\S+@\S+\.\S+/.test(form.email)||form.password.length<6){setError("Enter a valid email and a password of at least 6 characters.");return;}if(login(form.email,form.password))nav(loc.state?.from||"/dashboard");else setError("Email or password is incorrect.");};return <AuthShell title="Welcome back" sub="Sign in to continue your journey."><form className="auth-form" onSubmit={submit}>{error&&<div className="alert error">{error}</div>}<label>Email address<input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="you@example.com"/></label><label>Password<input type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="At least 6 characters"/></label><button className="btn primary full">Login</button><p className="auth-switch">New to TravelMate? <Link to="/register">Create an account</Link></p></form></AuthShell>}export function AuthShell({title,sub,children}){return <section className="auth-page"><div className="auth-visual"><div><Plane size={34}/><h2>Go where you feel most alive.</h2><p>Save the places that inspire you and build your next adventure.</p></div></div><div className="auth-panel"><div className="auth-box"><span className="eyebrow teal">TRAVELMATE</span><h1>{title}</h1><p>{sub}</p>{children}</div></div></section>}