import { Heart, CalendarDays, LogOut, Mail, UserRound } from "lucide-react"; import { useNavigate } from "react-router-dom"; import { useApp } from "../context/AppContext";
export default function Profile(){const {user,favourites,trips,logout}=useApp();const nav=useNavigate();const leave=()=>{logout();nav("/")};return <section className="page section profile-page"><div className="profile-card"><div className="avatar">{user.name.split(" ").map(x=>x[0]).slice(0,2).join("").toUpperCase()}</div><span className="eyebrow teal">TRAVELLER PROFILE</span><h1>{user.name}</h1><p><Mail size={16}/>{user.email}</p><div className="stats"><div><Heart/><strong>{favourites.length}</strong><span>Favourite places</span></div><div><CalendarDays/><strong>{trips.length}</strong><span>Planned trips</span></div></div><div className="profile-note"><UserRound/><div><b>Your TravelMate space</b><p>Your account and travel data are stored locally in this browser for this beginner-friendly demo.</p></div></div><button className="btn danger-btn" onClick={leave}><LogOut size={18}/>Logout</button></div></section>}