import { useState } from "react"; import { Edit3, Plus, Trash2, X } from "lucide-react"; import { useApp } from "../context/AppContext"; import { uid } from "../utils/storage";
const blank={name:"",location:"",description:"",image:"",category:"City",notes:""};
export default function Dashboard(){const {user,favourites,saveFavourite,deleteFavourite}=useApp();const [form,setForm]=useState(blank);const [editing,setEditing]=useState(false);const [show,setShow]=useState(false);const open=(p=blank)=>{setForm(p);setEditing(!!p.id);setShow(true)};const submit=e=>{e.preventDefault();saveFavourite({...form,id:form.id||uid(),image:form.image||"https://images.unsplash.com/photo-1488646953014-85cb44e25828?auto=format&fit=crop&w=1000&q=80"});setShow(false);setForm(blank)};return <section className="page section"><div className="dashboard-head"><div><span className="eyebrow teal">YOUR TRAVEL SPACE</span><h1>Welcome, {user.name.split(" ")[0]} 👋</h1><p>Keep your favourite places close and your travel ideas organized.</p></div><button className="btn primary" onClick={()=>open()}><Plus size={18}/>Add favourite</button></div>{favourites.length?<div className="saved-grid">{favourites.map(p=><article className="saved-card" key={p.id}><img src={p.image} alt={p.name}/><div><span className="category inline">{p.category}</span><h3>{p.name}</h3><small>{p.location}</small><p>{p.description}</p>{p.notes&&<p className="note">“{p.notes}”</p>}<div className="card-actions"><button onClick={()=>open(p)}><Edit3 size={16}/>Edit</button><button className="danger" onClick={()=>confirm("Delete this favourite?")&&deleteFavourite(p.id)}><Trash2 size={16}/>Delete</button></div></div></article>)}</div>:<div className="empty"><span>♡</span><h3>No favourite places yet</h3><p>Explore destinations or add a place you already love.</p><button className="btn primary" onClick={()=>open()}><Plus size={17}/>Add your first place</button></div>}{show&&<div className="modal-backdrop"><div className="modal"><button className="modal-close" onClick={()=>setShow(false)}><X/></button><h2>{editing?"Edit favourite":"Add favourite place"}</h2><form className="form-grid" onSubmit={submit}><label>Place name<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Location<input required value={form.location} onChange={e=>setForm({...form,location:e.target.value})}/></label><label>Category<select value={form.category} onChange={e=>setForm({...form,category:e.target.value})}>{["Beach","Mountains","Historical","Adventure","City"].map(x=><option key={x}>{x}</option>)}</select></label><label>Image URL<input value={form.image} onChange={e=>setForm({...form,image:e.target.value})}/></label><label className="span2">Description<textarea required value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></label><label className="span2">Personal notes<textarea value={form.notes} onChange={e=>setForm({...form,notes:e.target.value})}/></label><button className="btn primary span2">{editing?"Save changes":"Add favourite"}</button></form></div></div>}</section>}