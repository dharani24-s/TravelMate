import { useState } from "react"; import { MapPin, Navigation, Search } from "lucide-react";
export default function MapPage(){const [input,setInput]=useState("Taj Mahal, Agra");const [destination,setDestination]=useState("Taj Mahal, Agra");const go=e=>{e.preventDefault();setDestination(input.trim()||destination)};const encoded=encodeURIComponent(destination);return <section className="page section"><div className="page-title"><span className="eyebrow teal">MAP NAVIGATION</span><h1>Find your way</h1><p>Search a destination, preview it on the map and open turn-by-turn navigation.</p></div><form className="map-search" onSubmit={go}><Search/><input value={input} onChange={e=>setInput(e.target.value)} placeholder="Enter a destination"/><button className="btn primary">Show on map</button></form><div className="map-layout"><div className="map-frame"><iframe title="Destination map" src={`https://www.openstreetmap.org/export/embed.html?bbox=68.1%2C6.5%2C97.4%2C35.7&layer=mapnik`} loading="lazy"></iframe></div><aside className="route-card"><span className="route-icon"><MapPin/></span><small>SELECTED DESTINATION</small><h2>{destination}</h2><p>Open Google Maps to choose your starting point and get live route options.</p><a className="btn primary full" target="_blank" rel="noreferrer" href={`https://www.google.com/maps/dir/?api=1&destination=${encoded}`}><Navigation size={18}/>Navigate Now</a><div className="weather-box"><b>☀ Weather info</b><p>Weather placeholder — connect your preferred weather API here later.</p></div></aside></div></section>}